@model ResetPasswordVm
@{
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
    ViewData["Title"] = "Reset Password";
}

<div class="container mt-5" style="max-width:700px;">
    <div class="card shadow">
        <div class="row g-0">
            <!-- LEFT: Image -->
            <div class="col-md-5 d-none d-md-block">
                <img src="~/images/architecture-2256489.jpg" class="img-fluid h-100 w-100" style="object-fit: cover; border-radius: 6px 0 0 6px;" />
            </div>

            <!-- RIGHT: Form -->
            <div class="col-md-7 p-4">
                <h3 class="text-center mb-4">Reset Password</h3>

                <form asp-action="ResetPassword" method="post">
                    <input type="hidden" asp-for="Token" />
                    <input type="hidden" asp-for="Email" />

                    <div class="mb-3">
                        <label class="form-label">New Password</label>
                        <input asp-for="NewPassword" type="password" class="form-control" placeholder="Enter new password" />
                        <span asp-validation-for="NewPassword" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Confirm Password</label>
                        <input asp-for="ConfirmPassword" type="password" class="form-control" placeholder="Confirm new password" />
                        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                    </div>

                    <button type="submit" class="btn btn-success w-100 mt-3">Reset Password</button>

                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="text-danger mt-2">
                            @ViewData.ModelState[string.Empty]?.Errors.FirstOrDefault()?.ErrorMessage
                        </div>
                    }

                    @if (ViewBag.Message != null)
                    {
                        <div class="text-success mt-2">@ViewBag.Message</div>
                    }

                    <div class="text-center mt-3">
                        <a asp-action="Login" asp-controller="Account">Back to Login</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
